---
- hosts: all
  tasks:
    - name: install {{cookiecutter.project_name}}
      import_role:
        name: {{cookiecutter.project_slug}}
#      vars:
#        {{ cookiecutter.project_slug }}_deps_install: true
